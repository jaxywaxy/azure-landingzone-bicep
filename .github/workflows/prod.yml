name: Deploy Prod

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write
  contents: read
  pull-requests: write

concurrency:
  group: bicep-prod
  cancel-in-progress: true

jobs:
  bicep:
    runs-on: ubuntu-latest
    environment:
      name: prod

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Bicep Build
        run: az bicep build --file envs/prod/main.bicep

      - name: Bicep Validate (cloud-side)
        run: |
          az deployment sub validate \
            --name "bicep-validate-prod-$(date +%s)" \
            --location australiaeast \
            --template-file envs/prod/main.bicep \
            --parameters envs/prod/parameters.json

      - name: Bicep What-If
        id: whatif
        run: |
          az deployment sub what-if \
            --name "bicep-whatif-prod-$(date +%s)" \
            --location australiaeast \
            --template-file envs/prod/main.bicep \
            --parameters envs/prod/parameters.json \
            --result-format ResourceIdOnly \
            --no-pretty-print > envs/prod/whatif-output.txt

      - name: Comment What-If on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let whatif = fs.readFileSync('envs/prod/whatif-output.txt', 'utf8');
            const limit = 60000;
            if (whatif.length > limit) {
              whatif = whatif.slice(0, limit) + '\n\n[truncated — see job logs for full output]';
            }
            const marker = '<!-- bicep-whatif-prod -->';
            const body = `${marker}\n### Bicep What-If (Prod)\n\n<details><summary>Show diff</summary>\n\n\`\`\`\n${whatif}\n\`\`\`\n\n</details>`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find(c => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

      - name: Bicep Deploy
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        run: |
          az deployment sub create \
            --name "bicep-deploy-prod-$(date +%s)" \
            --location australiaeast \
            --template-file envs/prod/main.bicep \
            --parameters envs/prod/parameters.json
